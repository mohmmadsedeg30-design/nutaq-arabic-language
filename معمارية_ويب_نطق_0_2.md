# معمارية الويب للغة نُطْق 0.2

يوسّع الإصدار **0.2** نُطْق من لغة عامة الغرض إلى منصة ويب عربية متكاملة للنماذج الأولية والتطبيقات الصغيرة والمتوسطة. لا يعني ذلك أن اللغة ستنافس فورًا بيئات الويب الناضجة في الأداء أو العزل الأمني أو منظومات الحزم؛ بل إنها تقدم مسارًا حقيقيًا ومباشرًا لبناء موقع ديناميكي وتشغيله من ملف نُطْق واحد، مع فصل نظيف يتيح التطوير اللاحق.

> **مبدأ الطبقات:** تبقى صياغة التطبيق عربية في ملفات `.نطق`، وتنفَّذ تفاصيل HTTP والقوالب والملفات الثابتة في طبقة تشغيل مضيفة مستقلة. فلا تتسرب مصطلحات بايثون أو كائناته إلى واجهة المبرمج داخل اللغة.

| الطبقة | المسؤولية | الواجهة العربية |
|---|---|---|
| اللغة | الشروط والدوال والبيانات والنطاقات | `دالة`، `إذا`، قوائم وقواميس |
| التطبيق | تعريف المسارات ومنطق الصفحات | `مسار`، `صفحة`، `JSON`، `شغّل` |
| تشغيل الويب | HTTP وتحليل الطلب والأمان الأساسي للمسارات | غير ظاهر مباشرة للمبرمج |
| العرض | ملفات HTML وقوالب آمنة افتراضيًا | `قالب("الملف.html"، السياق)` |
| الأصول | CSS وJavaScript والصور | `ثابت("/ثابت"، "static")` |

## دورة الطلب

عند وصول طلب HTTP، يحلل المشغّل المسار وسلسلة الاستعلام والجسم والرؤوس، ويحوّلها إلى قاموس نُطْق باسم `طلب`. ثم يطابق المشغّل المسار المسجّل، بما في ذلك الأجزاء ذات الأسماء مثل `/مقال/{معرّف}`، ويستدعي دالة نُطْق المقابلة بوسيط واحد. يجب أن تعيد الدالة استجابة منشأة بـ`صفحة` أو `JSON` أو `نص` أو `إعادة_توجيه`. وتحوّل طبقة التشغيل هذه القيمة إلى استجابة HTTP.

| مفتاح `طلب` | المحتوى |
|---|---|
| `طريقة` | طريقة HTTP، مثل `GET` أو `POST`. |
| `مسار` | المسار المفكوك الترميز. |
| `معاملات` | قاموس معاملات المسار المسماة. |
| `استعلام` | قاموس سلسلة الاستعلام؛ القيمة الأولى لكل اسم. |
| `رؤوس` | قاموس الرؤوس الواردة. |
| `نص` | جسم الطلب كنص UTF-8. |
| `بيانات` | قاموس JSON أو بيانات نموذج عند إمكان تحليل الجسم؛ وإلا `عدم`. |

## واجهة الويب المدمجة

| الدالة | الغرض |
|---|---|
| `مسار(طريقة، نمط، معالج)` | تسجيل معالج لمسار HTTP؛ يقبل `GET` و`POST` و`PUT` و`PATCH` و`DELETE`. |
| `صفحة(HTML، حالة?، رؤوس?)` | إنشاء استجابة HTML بـUTF-8. |
| `استجابة_نص(محتوى، حالة?، رؤوس?)` | إنشاء استجابة نصية؛ ويظل `نص(قيمة)` مخصصًا لتحويل القيم إلى نص. |
| `JSON(بيانات، حالة?)` | إنشاء واجهة JSON؛ ترميز عربي دون هروب ASCII. |
| `إعادة_توجيه(وجهة، حالة?)` | إعادة توجيه HTTP؛ الحالة الافتراضية 302. |
| `قالب(اسم، سياق?)` | عرض قالب من مجلد `قوالب/`، مع هروب HTML للقيم. |
| `ثابت(بادئة، مجلد?)` | تقديم ملفات ثابتة آمنة من مجلد محلي. |
| `شغّل(منفذ?)` | بدء خادم HTTP محلي؛ المنفذ الافتراضي 8000. |

## القوالب والأمان الافتراضي

القالب ملف HTML عادي في المسار `قوالب/` نسبة إلى ملف التطبيق. تستخدم `{{اسم}}` لاستبدال قيمة مُهربة بتقنية HTML، وتحجب الأحرف الخاصة كي لا تفسر كوسوم أو شيفرات. وتُتاح `{{{اسم}}}` فقط عند الحاجة إلى HTML موثوق مكتمل؛ وهي متعمدة الصراحة لأنها تتجاوز الهروب.

لا يسمح مشغّل الملفات الثابتة أو القوالب بمغادرة مجلد المشروع عبر مسارات مثل `../`، كما يربط نوع المحتوى بامتداد الملف ويمنع التخمين الخطر للمسارات. لا يوفر الإصدار الأول جلسات أو مصادقة أو قاعدة بيانات أو حماية CSRF؛ وهي طبقات مخطط لها ويجب عدم ادعاء جاهزية الخادم المكشوف للإنترنت قبل إضافتها ومراجعتها أمنيًا.

## بنية مشروع مقترحة

```text
موقعي/
├── التطبيق.نطق
├── قوالب/
│   └── الرئيسية.html
└── static/
    └── style.css
```

## معيار القبول

يكتمل الامتداد عندما يعرّف ملف نُطْق مسار صفحة ومسار JSON ومسارًا بمعامل، ويعرض قالبًا محليًا، ويخدم CSS ثابتًا، ويُتحقق منه بطلبات HTTP آلية. سيضاف مشروع مستقل باسم `مثال_ويب/` يحقق هذه الشروط ويُشغّل بأمر واحد.
