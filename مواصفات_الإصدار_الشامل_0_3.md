# مواصفات نُطْق الشاملة 0.3

يحوّل الإصدار **0.3** نُطْق إلى بيئة برمجة عربية أوسع للتطبيقات العامة والويب. تضاف إلى النواة الحالية الوحدات والاستثناءات والصفوف والخصائص، ومكتبة للتعامل الآمن نسبيًا مع الملفات وJSON وSQLite، وتطبيق ويب CRUD كامل. يركز هذا الإصدار على أن تكون رحلة بناء تطبيق عربي — من الملف إلى البيانات والواجهة — ممكنة داخل اللغة نفسها.

> **الالتزام الواقعي:** هذا إصدار مكتمل وظيفيًا للتعلّم والتطوير المحلي وبناء النماذج والتطبيقات الصغيرة والمتوسطة. لا يساوي ذلك تلقائيًا منصة إنتاج عامة؛ فالنشر العام يتطلب طبقات مصادقة وجلسات وحماية CSRF وإدارة أسرار وحدود موارد ومراجعة أمنية مستقلة.

| المحور | الإضافة في 0.3 | الغاية العملية |
|---|---|---|
| التنظيم | `استورد "ملف.نطق" ك وحدة` | فصل الكود إلى ملفات ومكتبات محلية. |
| الأخطاء | `حاول` و`التقط` و`أخيرًا` و`ارم` | بناء تدفقات فشل يمكن إدارتها داخل التطبيق. |
| النمذجة | `صنف` وخصائص وطرائق | تمثيل الكيانات والخدمات بصورة منظمة. |
| ملفات وبيانات | قراءة وكتابة مقيدة بمجلد المشروع، وتحويل JSON | حفظ الإعدادات والبيانات المحلية بأسماء عربية. |
| قاعدة بيانات | SQLite عبر `افتح_قاعدة` و`نفذ` | تطبيقات CRUD محلية بلا خادم قاعدة خارجي. |
| الويب | نموذج مهام متكامل مع صفحة وAPI وPOST/DELETE | برهان عملي لمسار موقع متكامل. |

## الصياغة الجديدة

```نطق
استورد "مكتبات/تحقق.نطق" ك تحقق

صنف مهمة {
    دالة تهيئة(ذات، عنوان) {
        ذات.عنوان = عنوان
        ذات.مكتملة = خطأ
    }

    دالة أتمم(ذات) {
        ذات.مكتملة = صحيح
    }
}

حاول {
    إذا ليس تحقق.عنوان_صحيح("تعلم نُطْق") {
        ارم "العنوان غير صالح"
    }
} التقط خطأ {
    اطبع(خطأ["رسالة"])
} أخيرًا {
    اطبع("انتهت المحاولة")
}
```

| البناء | المعنى |
|---|---|
| `استورد "مسار" ك اسم` | ينفّذ ملف نُطْق محليًا مرة واحدة ويعرض تعريفاته عبر `اسم.تعريف`. يمنع المسار من مغادرة مجلد المشروع. |
| `صنف اسم { دالة … }` | ينشئ نوعًا قابلًا للاستدعاء. تستدعى `تهيئة` تلقائيًا عند إنشاء كائن إن وُجدت. |
| `ذات.حقل` | قراءة خاصية كائن أو وحدة أو قيمة قاموسية ذات اسم مطابق. |
| `ذات.حقل = قيمة` | تعيين خاصية كائن أو مفتاح قاموس. |
| `حاول {…} التقط خطأ {…}` | يلتقط خطأ تشغيل أو قيمة صريحة من `ارم` في قاموس فيه `نوع` و`رسالة` و`قيمة`. |
| `أخيرًا {…}` | ينفّذ بعد نجاح كتلة المحاولة أو فشلها. |

## المكتبة المعيارية الموسعة

| الدالة أو القيمة | العقد |
|---|---|
| `إلى_JSON(قيمة)` / `من_JSON(نص)` | تحويل بيانات القوائم والقواميس والأعداد والنصوص والمنطقيات و`عدم` إلى JSON أو منه. |
| `اقرأ_ملف(مسار)` / `اكتب_ملف(مسار، محتوى)` | قراءة وكتابة UTF-8 داخل مجلد المشروع فقط؛ ترفض المسار المطلق أو الخروج بـ`..`. |
| `يوجد(مسار)` | اختبار وجود ملف أو مجلد داخل المشروع. |
| `افتح_قاعدة(مسار)` | فتح قاعدة SQLite داخل المشروع وإرجاع كائن قاعدة. |
| `قاعدة.نفذ(SQL، معاملات?)` | تنفيذ SQL بمعاملات قائمة؛ يعيد صفوف `SELECT` على هيئة قائمة قواميس ويلتزم المعاملات بدل بناء النصوص. |
| `قاعدة.أغلق()` | إغلاق اتصال SQLite. |
| `رتب(قائمة)` / `اعكس(قائمة)` / `انضم(قائمة، فاصل?)` | عمليات قائمة مساعدة لا تغير الأصل. |
| `هروب_HTML(قيمة)` | يحوّل الأحرف الخاصة إلى تمثيل HTML آمن عند بناء HTML ديناميكي يدويًا. |

## حدود السلامة

تعمل دوال الملفات وقواعد البيانات ضمن جذر المشروع الذي يحتوي ملف التطبيق. لا تُنفَّذ تعليمات SQL من مستخدم غير موثوق، ويجب تمرير القيم في المعامل الثاني لـ`نفذ` بدل دمجها يدويًا في النص. تعالج القوالب HTML بقيم مهربة افتراضيًا، ويبقى الإدراج الخام `{{{اسم}}}` مقتصرًا على HTML موثوق أنشأه التطبيق.

## معيار القبول

يقبل الإصدار برنامجًا يحمّل وحدة محلية، وينشئ كائنًا من صف، ويلتقط خطأ، ويقرأ JSON ويكتبه، وينفذ CRUD عبر SQLite، ويخدم تطبيق ويب يستخدم هذه الخصائص. وتغطي الاختبارات كل محور مع طلبات HTTP آلية.
