# مواصفات لغة نُطْق 0.1

تعرّف هذه الوثيقة الصياغة والسلوك التنفيذي للإصدار الأول من لغة **نُطْق**. الكلمات المحجوزة حساسة لحروفها، أما المعرّفات فلا تُحوَّل تلقائيًا بين صور الحروف العربية؛ لذلك يعد `اسم` و`إسم` معرفين مختلفين. يتجاهل المفسّر الفراغات والتبويبات، ويُعامل نهاية السطر وفاصلة التعليمة `؛` على أنهما فاصلان للتعليمات خارج القوائم والأقواس.

## الكلمات المحجوزة والرموز

| الفئة | العناصر |
|---|---|
| التعريف والتحكم | `دع`، `دالة`، `أرجع`، `إذا`، `وإلا`، `بينما`، `لكل`، `في`، `توقف`، `تابع` |
| القيم المنطقية والخاصة | `صحيح`، `خطأ`، `عدم` |
| العوامل المنطقية | `و`، `أو`، `ليس` |
| العوامل الحسابية | `+`، `-`، `*`، `/`، `%`، `^` |
| عوامل المقارنة | `==`، `!=`، `<`، `<=`، `>`، `>=` |
| البنية | `(`، `)`، `[`، `]`، `{`، `}`، `,` أو `،`، `:`، `=`، `;` أو `؛` |

تبدأ التعليقات بالرمز `#` وتمتد إلى آخر السطر. تقبل النصوص علامتي الاقتباس المفردة أو المزدوجة وتدعم المحارف المهروبة `\n` و`\t` و`\\` و`\"` و`\'`.

> **المعرّف** سلسلة تبدأ بحرف يونيكود أو شرطة سفلية `_`، وتتبعها حروف أو أرقام أو شرطات سفلية. ومن ثم تكون أسماء مثل `مجموع_2026` و`درجة` و`value_2` صحيحة.

## القواعد النحوية

تستخدم هذه الصياغة الرموز `*` للتكرار و`?` للاختيار و`|` للبدائل. الرمز `فاصل` يعني نهاية سطر واحدة أو أكثر أو فاصلة منقوطة عربية/لاتينية واحدة أو أكثر.

```ebnf
برنامج          = (تعليمة فاصل*)* EOF ;
تعليمة          = تعريف | إسناد | تعبير | أرجع | إذا | بينما | لكل | توقف | تابع ;
تعريف           = "دع" معرف ("=" تعبير)? ;
إسناد           = هدف "=" تعبير ;
هدف             = معرف | وصول ;
أرجع            = "أرجع" تعبير? ;
توقف            = "توقف" ;
تابع            = "تابع" ;

إذا              = "إذا" تعبير كتلة ("وإلا" (إذا | كتلة))? ;
بينما            = "بينما" تعبير كتلة ;
لكل              = "لكل" معرف "في" تعبير كتلة ;
دالة             = "دالة" معرف "(" معاملات? ")" كتلة ;
معاملات          = معرف (فاصلة معرف)* ;
كتلة             = "{" فاصل* (تعليمة فاصل*)* "}" ;

تعبير            = أو ;
أو               = و ("أو" و)* ;
و                = مساواة ("و" مساواة)* ;
مساواة           = مقارنة (("==" | "!=") مقارنة)* ;
مقارنة            = جمع (("<" | "<=" | ">" | ">=") جمع)* ;
جمع              = ضرب (("+" | "-") ضرب)* ;
ضرب              = أس (("*" | "/" | "%") أس)* ;
أس               = أحادي ("^" أس)? ;
أحادي            = ("-" | "ليس") أحادي | نداء ;
نداء             = أولي (("(" وسائط? ")") | ("[" تعبير "]"))* ;
وسائط            = تعبير (فاصلة تعبير)* ;
أولي             = عدد | نص | "صحيح" | "خطأ" | "عدم" | معرف | قائمة | قاموس | "(" تعبير ")" ;
قائمة            = "[" (تعبير (فاصلة تعبير)*)? "]" ;
قاموس            = "{" (تعبير ":" تعبير (فاصلة تعبير ":" تعبير)*)? "}" ;
فاصلة            = "," | "،" ;
```

## الدلالات التنفيذية

| العنصر | السلوك المحدد |
|---|---|
| العدد | يقبل أعدادًا صحيحة وعشرية موجبة أو سالبة. القسمة تنتج عددًا عشريًا. |
| النص | يدعم الجمع مع نص آخر، والفهرسة بعدد صحيح، والمرور عليه داخل `لكل`. |
| القائمة | قابلة للفهرسة والإسناد عبر `قائمة[فهرس] = قيمة`، وقابلة للمرور. |
| القاموس | مفاتيحه يجب أن تكون قابلة للتجزئة في بيئة التنفيذ، ويدعم `قاموس[مفتاح]`. |
| الصدق | `عدم` و`خطأ` والصفر والنص/القائمة/القاموس الفارغ قيم كاذبة؛ وما عداها صادق. |
| `و` و`أو` | قصيرا الدارة؛ يعيد `و` أول قيمة كاذبة أو الأخيرة، ويعيد `أو` أول قيمة صادقة أو الأخيرة. |
| النطاق | ينشئ كل بلوك نطاقًا داخليًا. يبحث الإسناد عن اسم موجود في النطاق المحيط قبل إنشاء اسم محلي، بينما يفرض `دع` إنشاء اسم في النطاق الحالي. |
| الدوال | تحفظ الدالة البيئة الموجودة عند تعريفها، وتنشئ نطاق استدعاء للمعاملات. و`أرجع` يخرج من أقرب دالة فقط. |
| الحلقات | يعمل `توقف` و`تابع` داخل أقرب حلقة فقط. تعبر `لكل` عناصر القائمة والنص والمفاتيح في القاموس. |

## الدوال المضمنة

| الدالة | الوصف |
|---|---|
| `اطبع(… )` | تطبع القيم مفصولة بمسافة ثم سطر جديد. |
| `أدخل(رسالة?)` | تقرأ سطرًا من المستخدم. |
| `طول(قيمة)` | تعيد طول نص أو قائمة أو قاموس. |
| `نطاق(بداية، نهاية?، خطوة?)` | تعيد قائمة أعداد صحيحة؛ تشبه نطاق العد المعتاد مع حد نهاية غير شامل. |
| `عدد(قيمة)` | تحول النص أو المنطقي إلى عدد إن أمكن. |
| `نص(قيمة)` | تحول القيمة إلى تمثيل نصي في نُطْق. |
| `نوع(قيمة)` | تعيد اسم النوع العربي: `عدد` أو `نص` أو `منطقي` أو `قائمة` أو `قاموس` أو `عدم` أو `دالة`. |
| `أضف(قائمة، قيمة)` | تضيف قيمة في نهاية قائمة وتعيد `عدم`. |
| `احذف(قائمة، فهرس)` | تحذف عنصر قائمة بالفهرس وتعيد العنصر المحذوف. |
| `مفاتيح(قاموس)`، `قيم(قاموس)` | تعيدان قائمة المفاتيح أو القيم. |

## الأخطاء

سيتوقف التنفيذ عند أول خطأ، وستحمل رسالة الخطأ نوعه ورقم السطر والعمود ما أمكن. الأخطاء الأساسية هي: `خطأ_رمزي` لمحرف غير قانوني، و`خطأ_نحوي` لبنية غير مكتملة، و`خطأ_اسم` لمعرف غير معلوم، و`خطأ_نوع` لعملية لا تنطبق على النوع، و`خطأ_فهرس` لفهرس خارج الحدود، و`خطأ_تنفيذي` لباقي الأخطاء القابلة للتشخيص.

## أمثلة قصيرة

```نطق
# دالة تراجعية
دالة عاملي(ن) {
    إذا ن <= 1 { أرجع 1 }
    أرجع ن * عاملي(ن - 1)
}

دع درجات = {"سارة": 98، "ليان": 91}
لكل اسم في مفاتيح(درجات) {
    اطبع(اسم + ": " + نص(درجات[اسم]))
}

اطبع("5! = " + نص(عاملي(5)))
```
