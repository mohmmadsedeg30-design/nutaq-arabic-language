# نُطْق طرفية 0.5 — برمجة نصية عربية في Termux

يضيف الإصدار 0.5 طبقة طرفية خفيفة إلى نُطْق، فتغدو اللغة مناسبة للبرامج النصية التفاعلية: الاستقبال من المستخدم، والطباعة الملونة، ومسح الشاشة، والانتظار، والعد التنازلي، والاستعلام عن عرض الطرفية، والخروج المنظم. تعتمد الطبقة على تسلسلات ANSI القياسية ومكتبة بايثون القياسية فقط، ولذلك لا تحتاج إلى تثبيت مكتبات إضافية في Termux.

> **اتجاه العربية:** تتحكم واجهة Termux ومحرك الخط فيها في الموضع البصري النهائي للحروف. لا تستطيع لغة أو برنامج فرض دعم ثنائي الاتجاه على كل محاكي طرفية. لذلك تضع نُطْق علامات اتجاه Unicode حول النصوص العربية عند تشغيل البرنامج النصي من واجهة الأوامر، وتوفر `اطبع_يمين` لمحاذاة النص إلى عرض الطرفية. هذا أفضل دعم برمجي ممكن مع بقاء التوافق مع الطرفيات المختلفة.

| الدالة | الغرض |
|---|---|
| `لوّن(نص، لون)` | تغليف النص بلون ANSI؛ الألوان العربية هي `رمادي` و`أحمر` و`أخضر` و`أصفر` و`أزرق` و`أرجواني` و`سماوي` و`أبيض`. |
| `أحمر(نص)` وما شابه | اختصار مباشر لتلوين الرسائل. |
| `امسح()` | مسح الشاشة وإعادة المؤشر إلى البداية. |
| `انتظر(ثوان)` | إيقاف التنفيذ لعدد ثوانٍ غير سالب. |
| `عرض_الطرفية()` و`ارتفاع_الطرفية()` | أبعاد الطرفية الحالية؛ تستخدم قيمًا افتراضية آمنة إن لم تتوفر. |
| `كرر(نص، عدد)` | تكرار نص، وهو مفيد للخطوط والتأثيرات النصية. |
| `اكتب(قيمة، نهاية?)` | طباعة بلا سطر جديد افتراضيًا؛ تقبل مثلًا `"\r"` للعد التنازلي. |
| `اطبع_يمين(قيمة، لون?)` | محاذاة الرسالة العربية إلى اليمين مع علامات الاتجاه المساعدة. |
| `نص_يمين(نص)` | إرجاع نص محاط بعلامات اتجاه عربية للاستخدام المخصص. |
| `أدخل_يمين(رسالة?)` | قراءة إدخال مع رسالة عربية مساعدة الاتجاه. |
| `اخرج(حالة?)` | إنهاء البرنامج بحالة خروج اختيارية. |

## الصياغة العربية المكافئة للمثال

```نطق
دع الاسم = أدخل_يمين(أخضر("من فضلك أدخل اسمك: "))
اطبع_يمين("مرحبًا " + الاسم + "، أهلاً بك في المنصة."، "أزرق")
انتظر(2)
امسح()

دع العمر = عدد(أدخل_يمين(أخضر("أدخل عمرك: ")))
إذا العمر < 18 {
    اطبع_يمين("عذرًا، يجب أن يكون عمرك 18 عامًا أو أكثر."، "أحمر")
    اخرج()
}

دع كلمة_المرور = أدخل_يمين(أخضر("أدخل كلمة المرور: "))
إذا كلمة_المرور != "Dagger123" {
    اطبع_يمين("كلمة المرور غير صحيحة. سيغلق البرنامج بعد 10 ثوانٍ."، "أحمر")
    لكل ثانية في نطاق(10، -1، -1) {
        اكتب(لوّن("متبقي: " + نص(ثانية) + " ", "أصفر")، "\r")
        انتظر(1)
    }
    اخرج(1)
}

امسح()
دع خطأ_نصي = "خطأ  "
اطبع(أحمر(كرر(خطأ_نصي، عدد(عرض_الطرفية() / طول(خطأ_نصي)))))
```

تكتب برامج نُطْق في ملفات `.نطق` وتُشغّل في Termux بأمر `python -m nutaq.cli ملف.نطق`. تضيف واجهة الأوامر علامات اتجاه للنصوص التي تحتوي حروفًا عربية افتراضيًا؛ ويمكن تعطيل ذلك عند الحاجة عبر الخيار `--بدون-اتجاه-عربي`.
