# نُطْق واجهة 0.4 — مكتبة واجهات عربية خفيفة

تضيف هذه الطبقة مكتبة واجهات مدمجة إلى منصة ويب نُطْق، بهدف تمكين المبرمج من بناء مواقع عربية جميلة وتفاعلية من دون تنزيل إطار واجهات خارجي. تقوم المكتبة على CSS وJavaScript صغيرين يقدّمهما الخادم من مسار داخلي، وعلى دوال نُطْق تُنتج مكوّنات HTML مضبوطة. لا تعتمد المكتبة على صور أو خطوط أو مكتبات CDN، لذلك تبقى سريعة وصغيرة وقابلة للعمل دون اتصال بعد تنزيل المشروع.

> **مسار التصميم:** لأن الطلب هو إضافة مصدر برمجي وتفاعل فعلي داخل موقع، ستُبنى الواجهة كـHTML/CSS/JavaScript قابل للتعديل لا كصورة واجهة ثابتة. الأيقونات SVG مضمنة نصيًا وقابلة للتكبير دون ملفات صور إضافية.

| الفئة | ما ستوفره | الاستخدام داخل موقع نُطْق |
|---|---|---|
| السمات | ألوان فاتحة وداكنة ودعم تلقائي لتفضيل النظام | `<link href="/_نطق/ui.css">` أو `أصول_واجهة()` |
| الألوان | متغيرات CSS للون الأساسي والنجاح والتحذير والخطر والنص والسطح | فئات مثل `ن-زر` و`ن-بطاقة` |
| الأيقونات | رموز SVG عربية الأسماء: `منزل`، `قائمة`، `بحث`، `مستخدم`، `حذف`، `تعديل`، `تحقق` | `أيقونة("بحث")` |
| المكوّنات | زر، بطاقة، تنبيه، شارة، نافذة، زر نافذة، تبويبات | دوال نصية تستخدم في قالب HTML خام موثوق |
| التفاعل | فتح النوافذ وإغلاقها، تبديل السمة، تبديل التبويبات، إخفاء التنبيهات | ملف JavaScript داخلي صغير |

## الواجهة البرمجية العربية

| الدالة | العقد |
|---|---|
| `أصول_واجهة()` | تعيد وسمي `<link>` و`<script>` اللازمين؛ تدرج في `<head>` أو قبل إغلاق `<body>`. |
| `أيقونة(اسم، حجم?)` | تعيد SVG آمنًا؛ الأسماء المدعومة موثقة في الدليل. |
| `زر(نص، نمط?)` | ينشئ زرًا بنمط `أساسي` أو `ثانوي` أو `خطر` أو `نجاح`. |
| `بطاقة(عنوان، محتوى، تذييل?)` | تنشئ بطاقة ذات بناء دلالي ومرن. |
| `تنبيه(نص، نوع?)` | ينشئ تنبيهًا: `معلومات` أو `نجاح` أو `تحذير` أو `خطر`. |
| `شارة(نص، نوع?)` | شارة موجزة لحالة أو تصنيف. |
| `زر_نافذة(معرف، نص)` | زر يفتح نافذة ذات معرف محدد. |
| `نافذة(معرف، عنوان، محتوى)` | نافذة حوار قابلة للإغلاق بلوحة المفاتيح أو بزر الإغلاق. |
| `تبويبات(عناصر)` | عناصر تبويبات؛ كل عنصر قاموس فيه `عنوان` و`محتوى`. |

تُهَرَّب القيم النصية داخل هذه الدوال بتقنية HTML. أما `محتوى` البطاقة والنافذة ومحتوى التبويب فيعامل HTML موثوقًا أنشأه التطبيق؛ لذلك يجب أن يمرر أي نص قادم من المستخدم عبر `هروب_HTML` قبل إدراجه.

## قيود الحجم

تحتوي الأصول على CSS وJavaScript فقط، وتُضمّن الأيقونات كـSVG عند الطلب. لا توجد حزم node أو خطوط خارجية أو مكتبات تابعة. وسيبقى الأرشيف الكامل أقل بكثير من ميغابايت واحد، مع عدم إضافة أصول صور.
