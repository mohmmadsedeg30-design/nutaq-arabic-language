# سجل إصدارات نُطْق

## 0.5.0 — برمجة طرفية عربية

أضاف هذا الإصدار مكتبة طرفية عربية مدمجة للألوان ومسح الشاشة والانتظار والإدخال وحجم الطرفية والخروج المنظم. أصبح بالإمكان كتابة برامج تفاعلية في ملفات `.نطق` دون استيراد مكتبات بايثون، مع دوال `اطبع_يمين` و`أدخل_يمين` وعلامات Unicode مساعدة لتحسين عرض العربية عند التشغيل من Termux. يتضمن الإصدار برنامج دخول طرفيًا كاملاً يحوي إدخال الاسم والعمر وكلمة المرور وشروطًا وعدًا تنازليًا وتأثيرًا نصيًا. يظل اتجاه العرض النهائي خاضعًا لدعم محاكي الطرفية.

## 0.4.0 — مكتبة واجهات عربية خفيفة

أضاف هذا الإصدار مكتبة **نُطْق واجهة** المدمجة. تتضمن نظام ألوان فاتحًا وداكنًا، وأيقونات SVG مضمنة، وأزرارًا وبطاقات وتنبيهات وشارات ونوافذ وتبويبات. يقدّم الخادم أصول CSS وJavaScript من مسارات داخلية، ولا تتطلب المكتبة حزمًا خارجية أو خطوطًا أو صورًا أو CDN. كما أضيف معرض واجهة تطبيقي وحدّث تطبيق المهام ليستعمل أصول الواجهة وزر تبديل السمة.

## 0.3.0 — الإصدار الشامل

أضاف هذا الإصدار الوحدات المحلية باستخدام `استورد … ك …`، والصفوف والكائنات والطرائق والخصائص، ومعالجة الأخطاء باستخدام `حاول` و`التقط` و`أخيرًا` و`ارم`. كما أضاف مكتبة للملفات المحصورة في مجلد المشروع، وJSON، وSQLite بمعاملات، ودوال القوائم، وهروب HTML. ويتضمن تطبيق مهام عربيًا كاملًا يستخدم `GET` و`POST` و`PATCH` و`DELETE` وقاعدة بيانات محلية.

| مجال التحقق | التغطية |
|---|---|
| نواة اللغة | الحساب والدوال والتكرار والنطاقات وبنى البيانات والأخطاء. |
| اللغة الموسعة | الصفوف والطرائق والوحدات والاستثناءات وJSON والملفات وSQLite. |
| الويب | القوالب الآمنة، والأصول الثابتة، والمسارات الديناميكية، وطرق CRUD. |

## 0.2.0 — منصة الويب

أضاف هذا الإصدار خادم HTTP مدمجًا ومسارات وقوالب HTML وواجهات JSON وملفات ثابتة، مع موقع متجر تجريبي عربي.

## 0.1.0 — نواة اللغة

قدّم الإصدار الأول المحلّل والمفسّر العربيين، والأعداد والنصوص والقوائم والقواميس والدوال والحلقات والمكتبة المعيارية الأساسية.
